export const CONFIG = {
  // Brand Assets
  ASSETS: {
    realProfilePhoto: "host_photo_1.png",       // The real 6MB face photo
    logoPhoto: "wealth_profile_photo.png",      // The logo badge
  },
  
  // Strict Contrast Colors (Overrides Seed Defaults for legibility)
  COLORS: {
    hookText: "#F5F3EC",                        // Off-white for contrast
    hookHighlightBg: "#FAC775",                 // Vibrant Brand Amber for highlighted words
    hookHighlightText: "#0F2027",               // Dark text specifically ON the amber background for contrast
    outroText: "#FFFFFF",                       // Pure white for outro text
    outroAccent: "#FAC775",                     // Brand amber for outro accents
  },
  
  // Display settings
  STYLES: {
    subtitleDropShadow: "0 6px 12px rgba(0,0,0,0.6)", // Clean drop shadow, NO duplicate text-strokes
  }
};
